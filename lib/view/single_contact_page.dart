import 'package:flutter/material.dart';

class SingleContact extends StatelessWidget {
  const SingleContact({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}